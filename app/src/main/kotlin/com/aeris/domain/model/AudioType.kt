package com.aeris.domain.model

enum class AudioType {
    NONE,           // No audio, only vibration
    BINAURAL,       // Binaural beats for relaxation (e.g. 432Hz + 440Hz)
    MEDITATION,     // Meditative drone / ambient
    AFFIRMATION,    // Text-to-Speech affirmations
    ENERGETIC       // Tibetan bowls, bells for energy practices
}
